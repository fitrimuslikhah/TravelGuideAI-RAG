version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install -r requirements.txt

  build:
    commands:
      - pytest

artifacts:
  files:
    - '**/*'      #Membungkus semua file kode untuk di kirim ke CodeDeploy
